# SENTINEL-ADM — runtime requirements.
# The deterministic core (privacy, legal, radar, forensics, notify, config) is
# stdlib-only; these packages are needed only for the API and the UI.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
streamlit>=1.38,<2.0
httpx>=0.27,<1.0
